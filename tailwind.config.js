const theme = require('./themes')

module.exports = {
  future: {
    // removeDeprecatedGapUtilities: true,
    // purgeLayersByDefault: true,
  },
  purge: [],
  theme,
  variants: {},
  plugins: [],
}
